<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.VibeX" parent="Theme.Material3.Light.NoActionBar">
        <!-- Customize your dark theme here. -->
        <!-- <item name="colorPrimary">@color/my_dark_primary</item> -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/black</item>
    </style>

    <style name="NotificationTitle">
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="NotificationSubtitle">
        <item name="android:textColor">@color/md_grey_200</item>
    </style>

    <style name="IconStyle">
        <item name="android:tint">@color/white</item>
    </style>

    <style name="CustomIndicator" parent="Widget.Material3.BottomNavigationView.ActiveIndicator">
        <item name="android:color">@color/md_green_400</item>
    </style>
</resources>